appiversion: apps/v1
kind: deployment
metadata: 
 name: mysql
 labels: 
  app: mysql

spec:
 replicas: 4
 selector:
   matchLabels:
     app: mysql
 
template:
  metadata:  
   labels: 
    app: mysql
  spec:  
   containers:
    - name: db-cntr
      image:user mysql:latest
      env:
       - name: MYSQL_DATABASE
         value: "boot_work"
       - nmae: MYSQL_USER   
         value: "admin"
       - name: MYSQL_PASSWORD
         value: "root"
       - name: MYSQL_ROOT_PASSWORD
         value: "root"
      ports:
       - containerport: 3306
      VolumeMounts:
         - name: mysqldata
           mountpath: /var/lib/mysql
  Volumes:
     - name: mysqldata
       persistentVolumeClaim:
         claimName: mysql-pvc
      
      imagepullpolicy: Always